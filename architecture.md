{
  "status": "pending_approval",
  "agent": "system-architect",
  "design_overview": "Add one new file, password_generator.py, as a sibling module to the existing password_strength.py. It exposes a single public, pure function generate_password(length=12) -> str plus two public constants (MIN_LENGTH=8, MAX_LENGTH=1024). Internally the module is a straight-line pipeline with no classes, no state and no I/O: validate the length argument, build the four ASCII character-class pools from the standard-library `string` module, draw one guarantee character from each of the four classes, fill the remaining length-4 positions from the combined pool of all four classes, shuffle the whole buffer with a cryptographically secure Fisher-Yates using secrets.randbelow, and join and return the result. All randomness comes exclusively from the `secrets` module; the `random` module is not imported anywhere, not even random.SystemRandom, because acceptance criterion 'does not use the random module for any character selection or shuffling step' is stated at module level and random.SystemRandom lives inside the `random` module. The generator deliberately does not import password_strength at runtime: consistency with its rules is guaranteed structurally (every character in every pool is provably in the class the checker recognises) and verified behaviourally by tests that feed generated output into check_password_strength(). This mirrors the existing module's style (dependency-free, pure, no globals mutated, constants at the top, private helpers prefixed with underscore, docstring stating the Raises contract) so the two files read as one coherent pair. Total expected surface: roughly 60-90 lines including docstrings. No CLI, no logging, no config, no persistence.",
  "components": [
    "password_generator.py -- new module, sibling of password_strength.py, the entire deliverable. Public API: generate_password(length=12) -> str, MIN_LENGTH, MAX_LENGTH, exported via __all__.",
    "Character pool constants -- module-level immutable strings derived from the standard-library `string` module: _UPPER = string.ascii_uppercase, _LOWER = string.ascii_lowercase, _DIGITS = string.digits, _SPECIAL = string.punctuation, and _ALL = _UPPER + _LOWER + _DIGITS + _SPECIAL. Frozen at import, never mutated, never rebuilt per call.",
    "_validate_length(length) -- private input guard and the only place that raises. Rejects bool explicitly (bool is a subclass of int) and any non-int with TypeError; rejects ints outside [MIN_LENGTH, MAX_LENGTH] with ValueError. Returns nothing; callers proceed only on success.",
    "_secure_shuffle(chars) -- private in-place Fisher-Yates over a list of characters, drawing every index from secrets.randbelow(i + 1). The only ordering primitive in the module; no other code decides positions.",
    "generate_password(length=12) -- public entry point that composes the above: validate, draw four guarantee characters (one per required class) with secrets.choice, draw the remaining length-4 characters with secrets.choice over the combined _ALL pool, shuffle, ''.join, return.",
    "Consistency contract with password_strength.check_password_strength -- a design invariant, not code: every character of _SPECIAL satisfies (not ch.isalnum() and not ch.isspace()), and _UPPER/_LOWER/_DIGITS satisfy isupper/islower/isdigit respectively, so the checker's five rules are satisfied by construction. Enforced by test assertions over the pools rather than by a runtime assert (asserts are stripped under python -O).",
    "Test-facing verification surface -- the tests owned by the test runner exercise: default length 12; explicit lengths 8, 16, 40 and a sweep up to MAX_LENGTH; check_password_strength() returning rating 'strong' with empty reasons for all of them; TypeError/ValueError paths; and distinctness across many repeated calls at the same length."
  ],
  "data_flow": "Caller (another part of the application) imports password_generator and calls generate_password(length). Step 1: _validate_length(length) runs first and is the sole gate -- on failure it raises TypeError (non-int, including bool, float, str, None) or ValueError (int below MIN_LENGTH or above MAX_LENGTH) and nothing else executes, so no password value is ever created for a rejected input. Step 2: four guarantee characters are drawn, one each via secrets.choice(_UPPER), secrets.choice(_LOWER), secrets.choice(_DIGITS), secrets.choice(_SPECIAL), into a list. Step 3: the remaining length-4 positions are filled with secrets.choice(_ALL), i.e. every filler character can be any of the four classes, not a narrower or predictable set. Step 4: _secure_shuffle permutes the full list in place using secrets.randbelow, so no character class occupies a fixed or predictable position and the guarantee characters are indistinguishable from the fillers. Step 5: the list is joined into a str and returned to the caller by value. Nothing is printed, logged, written to disk, cached, memoised, appended to a module-level list, or otherwise retained; the local list and the string reference go out of scope when the function returns, leaving the caller as the only holder of the password. The module reads no input other than its single argument and the OS CSPRNG accessed through `secrets`.",
  "key_decisions": [
    {
      "decision": "Deliver exactly one new file, password_generator.py, containing one public function generate_password(length: int = 12) -> str, importable and callable directly, with no CLI entry point, no __main__ block, no argparse and no network or web surface.",
      "traces_to_requirement": "Requirement: 'The feature must be delivered as an importable, reusable function (not a CLI tool or web endpoint) suitable for other parts of the application to call directly' and 'Provide a function that generates and returns a random password as a string' / 'must accept an optional target length parameter defaulting to 12'. Also resolves open_question 3 (function name and signature) with a concrete answer."
    },
    {
      "decision": "Use string.punctuation as the special-character pool, rather than a hand-written literal such as '!@#$%^&*()-_=+[]{};:,.<>?'. string.punctuation is the standard-library ASCII punctuation set !\"#$%&'()*+,-./:;<=>?@[\\]^_`{|}~; every one of its 32 characters provably satisfies the checker's test (not ch.isalnum() and not ch.isspace()), it cannot drift or acquire a typo the way a hand-maintained literal can, and it needs no third-party data.",
      "traces_to_requirement": "Requirement: 'The definition of special character used by the generator must be consistent with the definition used by password_strength.py's check_password_strength() (currently: any character that is not alphanumeric and not whitespace), so that generator output and checker rules never disagree', plus 'The implementation must use only the Python standard library'. Directly resolves open_question 1: the pool is bounded and specific (string.punctuation), chosen as a provably safe subset of the checker's broad definition rather than allowing arbitrary non-alphanumeric Unicode."
    },
    {
      "decision": "Restrict all four pools to ASCII (string.ascii_uppercase, string.ascii_lowercase, string.digits, string.punctuation) even though the checker is Unicode-aware, accepting a strict subset of what the checker would rate 'strong'.",
      "traces_to_requirement": "Requirement: 'The generated password must, every time, satisfy all 5 rules currently enforced by check_password_strength()' and acceptance criterion 'Passing the generated password into check_password_strength() must always yield a rating of strong with an empty reasons list, for every supported target length'. A subset that is provably inside every rule is the cheapest way to make 'every time' true; nothing in requirements.md asks for non-ASCII output."
    },
    {
      "decision": "Import only `secrets` and `string`. Do not import `random` at all -- including random.SystemRandom.shuffle -- and instead implement the shuffle as an in-place Fisher-Yates whose swap index comes from secrets.randbelow(i + 1). Character selection uses secrets.choice, which is itself unbiased (it is built on randbelow, not on modulo reduction).",
      "traces_to_requirement": "Requirement: 'All randomness used to select characters and to determine character order must come from a cryptographically secure source (e.g. Python's secrets module); Python's standard random module (or any other non-cryptographic PRNG) must not be used anywhere in the generation logic', and acceptance criterion 'Inspection/testing of the implementation confirms it draws randomness only from a cryptographically secure source ... and does not use the random module for any character selection or shuffling step'. The criterion is phrased against the module, so an import of `random` in any form would fail an inspection-based check even though SystemRandom is CSPRNG-backed."
    },
    {
      "decision": "Use the guarantee-then-fill-then-shuffle strategy: four characters drawn one per required class, the remaining length-4 characters drawn from the combined all-class pool, then a single CSPRNG shuffle of the whole buffer. Do not use rejection sampling (generate everything from the combined pool and retry until all four classes appear).",
      "traces_to_requirement": "Requirement: 'Every character position in the output must be drawn from a combined pool of uppercase letters, lowercase letters, digits, and special characters (not just one mandatory character per required class padded from a narrower or predictable set)' plus 'The final character order in the output must be randomized/shuffled using a cryptographically secure method so that required character classes do not consistently appear in fixed, predictable positions'. Fillers come from the full combined pool and the shuffle makes every output position class-unpredictable, which is what the parenthetical rules out. Rejection sampling is rejected because it introduces a retry loop with no guaranteed termination bound, whereas this strategy has fixed, length-proportional runtime while still satisfying the same criteria."
    },
    {
      "decision": "Raise TypeError for a non-int length and ValueError for an out-of-range int length, with messages naming the offending value/type and the accepted range. Treat bool as a type error via an explicit isinstance(length, bool) check before the int check, since bool subclasses int. Do not coerce numeric strings, do not accept integral floats such as 12.0, and do not fall back to the default.",
      "traces_to_requirement": "Acceptance criteria 'Calling the function with a length less than 8 (e.g. 0, 1, 7, or a negative number) raises a clear, documented error and does not return a password' and 'Calling the function with a non-integer length (e.g. a string, float, or None) raises a clear, documented error and does not return a password', plus the requirement that invalid inputs are 'rejected with a clear, explicit error rather than silently coerced or defaulted'. Resolves open_question 4 by choosing the built-in TypeError/ValueError split rather than a custom exception class, matching password_strength.py which already raises a plain TypeError for a wrong-typed argument -- a custom exception would force every caller to import from this module to handle it."
    },
    {
      "decision": "Enforce an inclusive upper bound MAX_LENGTH = 1024 alongside MIN_LENGTH = 8, exposing both as public module constants and rejecting length > MAX_LENGTH with ValueError.",
      "traces_to_requirement": "Resolves open_question 2 ('Is there a maximum length that should be enforced'). Supported by acceptance criterion 'For every supported length from the minimum up to a reasonably large value ...', which presupposes a defined supported range; a named constant makes 'reasonably large' testable and bounds the memory a caller-supplied length can request. 1024 is far above the largest length named anywhere in requirements.md (40) and is a single-constant change if the Product Owner wants it different."
    },
    {
      "decision": "Do not exclude ambiguous-looking characters (0/O, 1/l/I) and do not add any pattern constraints such as forbidding consecutive repeats, minimum class counts beyond one, or entropy floors.",
      "traces_to_requirement": "Resolves open_questions 5 and 6 as out of scope. No requirement or acceptance criterion in requirements.md mentions readability or pattern constraints, and the acceptance bar is exactly 'satisfies all 5 password_strength.py rules' with randomized selection and order. Excluding characters would also shrink the pools and reduce entropy for no requested benefit."
    },
    {
      "decision": "password_generator.py does not import password_strength.py. The two modules stay independent; the rule-consistency guarantee is structural (pool membership) and is verified by tests calling check_password_strength() on generated output, not by a runtime cross-check or a generate-and-verify retry loop inside the function.",
      "traces_to_requirement": "Requirement 'The implementation must use only the Python standard library' combined with the module-style requirement that the function 'must not perform any I/O ... consistent with the pure, side-effect-free style of the existing password_strength.py module', and acceptance criterion 'Calling the function with no arguments returns a string of length 12 that satisfies all 5 password_strength.py rules (check_password_strength() on it returns rating strong with an empty reasons list)' -- which places the cross-module check in the test layer, where it belongs."
    },
    {
      "decision": "Keep the function pure: no print, no logging, no file or network writes, no module-level mutable state, no caching or memoisation of generated values, and no storage of the returned password anywhere in the module. Module-level names are immutable strings and ints only.",
      "traces_to_requirement": "Requirement: 'The function must not perform any I/O (no printing, logging, or writing the generated password anywhere) and must not retain the generated password in any persistent or global state, consistent with the pure, side-effect-free style of the existing password_strength.py module.'"
    },
    {
      "decision": "Mirror password_strength.py's file conventions: module docstring naming the public API and stating the no-dependency/pure guarantees, a constants block, underscore-prefixed private helpers, and a function docstring with explicit Args / Returns / Raises sections documenting both TypeError and ValueError.",
      "traces_to_requirement": "Requirement framing the feature as 'a companion utility to that module' and the acceptance criteria requiring 'a clear, documented error' for both the short-length and non-integer cases -- the documented part lands in the Raises section."
    }
  ],
  "risks": [
    "string.punctuation includes quote, backslash and backtick characters. Passwords containing them are correct per the checker but can break downstream consumers that interpolate them into shell commands, SQL, CSV or unquoted config. Mitigation is a docstring note that the output is an arbitrary printable-ASCII string and callers must quote/escape it; narrowing the pool instead would reduce entropy and is not requested.",
    "Guarantee-then-fill does not sample uniformly from the set of all valid passwords of the given length -- the four guaranteed classes are very slightly over-represented, most visibly at length 8. The effect is small relative to the pool size (94 characters) and is the standard trade for guaranteed rule satisfaction without a retry loop, but it should be a conscious accepted trade rather than an accident.",
    "MAX_LENGTH = 1024 is an architect's answer to an open question, not something the raw request stated. If the Product Owner intends unbounded lengths, this is a rejection risk at review time -- it is deliberately isolated to one named constant plus one validation branch so reversing it is cheap.",
    "The bool-is-an-int subtlety is easy to miss: without the explicit isinstance(length, bool) check, generate_password(True) would fall through the int check and surface as a ValueError instead of a TypeError, contradicting the 'non-integer length raises' criterion. Needs an explicit test.",
    "The acceptance criterion 'repeated calls produce different passwords' is inherently probabilistic. At length 8 collisions are astronomically unlikely but a test asserting strict all-distinct across N calls is technically a flaky assertion; the test should assert distinctness over a large sample rather than mathematical impossibility.",
    "'Does not use the random module' is an inspection-based criterion. A future refactor could reintroduce random.SystemRandom believing it is equivalent; a test that asserts 'random' is absent from the module's imports (or from its source text) is the cheap guard.",
    "secrets requires Python 3.6+. The existing password_strength.py has no imports at all and therefore no floor of its own, so this module raises the project's effective minimum Python version. Assumed acceptable; worth confirming at approval.",
    "The per-character secrets.choice loop makes one CSPRNG call per character plus one per shuffle step. Fine for the lengths in scope, but a caller generating passwords in a hot loop at MAX_LENGTH would notice; no batching optimisation is proposed because no performance requirement exists."
  ]
}
